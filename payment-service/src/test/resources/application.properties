server.port=0

spring.application.name=payment-service

spring.cloud.config.enabled=false

spring.jpa.hibernate.ddl-auto=validate
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/station_service_db
spring.datasource.username=postgres
spring.datasource.password=postgres

yookassa.shop-id=1401038
yookassa.secret-key=secret-key
order.exchange=order.events
order.paid.routing.key=order.paid
yookassa.return-url=http://localhost:8080

yookassa.webhook.secret=webhook-secret